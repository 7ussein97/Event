@{
    ViewData["Title"] = "رابط غير صالح";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="invalid-page">
    <div class="invalid-container">
        <div class="invalid-icon">🔐</div>
        
        <h1 class="invalid-title">رابط غير صالح</h1>
        
        <p class="invalid-message">
            عذراً، الرابط الذي تحاول الوصول إليه غير صالح أو منتهي الصلاحية.
        </p>

        <div class="reasons-box">
            <h3>قد يكون السبب:</h3>
            <ul>
                <li>الدعوة تم حذفها</li>
                <li>الرابط غير مكتمل</li>
                <li>رمز التعديل غير صحيح</li>
            </ul>
        </div>

        <a href="/" class="btn btn-home">
            🏠 العودة للرئيسية
        </a>
    </div>
</div>

@section Styles {
<style>
    .invalid-page {
        min-height: 100vh;
        background: linear-gradient(180deg, #0a0a12 0%, #0f0a15 100%);
        padding: 2rem;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .invalid-container {
        max-width: 500px;
        width: 100%;
        text-align: center;
    }

    .invalid-icon {
        font-size: 5rem;
        margin-bottom: 1.5rem;
        opacity: 0.7;
    }

    .invalid-title {
        font-size: 1.75rem;
        color: #EF4444;
        margin-bottom: 1rem;
    }

    .invalid-message {
        color: rgba(255, 255, 255, 0.7);
        font-size: 1rem;
        margin-bottom: 2rem;
        line-height: 1.8;
    }

    .reasons-box {
        background: rgba(239, 68, 68, 0.1);
        border: 1px solid rgba(239, 68, 68, 0.2);
        border-radius: 12px;
        padding: 1.5rem;
        margin-bottom: 2rem;
        text-align: right;
    }

    .reasons-box h3 {
        color: rgba(255, 255, 255, 0.8);
        font-size: 0.95rem;
        margin-bottom: 0.75rem;
    }

    .reasons-box ul {
        margin: 0;
        padding-right: 1.25rem;
        color: rgba(255, 255, 255, 0.6);
        font-size: 0.9rem;
    }

    .reasons-box li {
        margin-bottom: 0.5rem;
    }

    .btn-home {
        display: inline-block;
        padding: 1rem 2rem;
        background: linear-gradient(135deg, #D4AF37, #B8860B);
        color: #0f0a15;
        border-radius: 50px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.3s ease;
    }

    .btn-home:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 30px rgba(212, 175, 55, 0.3);
    }
</style>
}
